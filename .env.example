# Alpaca API credentials (get from https://app.alpaca.markets)
# Copy this file to .env and fill in your keys. Never commit .env.

APCA_API_KEY_ID=your_key_id_here
APCA_API_SECRET_KEY=your_secret_key_here

# Paper trading (true) or live (false). Default: true.
APCA_PAPER=true

# ------------------------------------------------------------------------------
# Optional: momentum bot (bot.py)
# ------------------------------------------------------------------------------
# Comma-separated tickers to watch and trade. Default: SPY.
# BOT_SYMBOLS=SPY,AAPL,QQQ

# Fast SMA length in days (default 10). Average of last N closing prices.
# BOT_FAST_SMA=10

# Slow SMA length in days (default 30).
# BOT_SLOW_SMA=30

# Minutes between each strategy check (default 15).
# BOT_INTERVAL_MINUTES=15

# Position sizing — use ONE of the two below.
# BOT_POSITION_SIZE: fixed number of shares per buy (default 1).
# BOT_POSITION_DOLLARS: target $ per buy; bot converts to shares using latest price (e.g. 5000 = ~$5k per symbol).
# For large accounts (~$100k), many use 2–5% per position ($2k–$5k) to limit risk. No setting guarantees profit.
# BOT_POSITION_SIZE=1
# BOT_POSITION_DOLLARS=5000

# Paper (true) or live (false) for the bot. Defaults to APCA_PAPER if unset.
# BOT_PAPER=true

# ------------------------------------------------------------------------------
# Telegram — alerts and reports use the same token and chat_id
# ------------------------------------------------------------------------------
# Token from @BotFather. Chat ID: message your bot, then run: python3 telegram_get_chat_id.py
# TELEGRAM_BOT_TOKEN=your_bot_token_from_botfather
# TELEGRAM_CHAT_ID=your_chat_id
